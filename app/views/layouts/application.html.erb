<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
  	<meta charset="utf-8" />

  	<!-- Uncomment to make IE8 render like IE7 -->
  	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

  	<!-- Set the viewport width to device width for mobile -->
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) : "AuditRailsExample" %></title>

  	<%= stylesheet_link_tag    "application" %>
  	<%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="fixed contain-to-grid">
      <nav class="top-bar" style="">
        <ul class="title-area">
          <!-- Title Area -->
          <li class="name">

          </li>
          <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
          <li class="toggle-topbar menu-icon"><a href=""><span>Menu</span></a></li>
        </ul>
        <section class="top-bar-section">
          <ul class="left">
            <li class="not-click <%= selected('home') %>"><%= link_to 'Home', root_url %></li>
            <li class="divider"></li>
            <li class="not-click"><%= link_to 'Analytics', audit_rails.analytics_audits_url %></li>
            <% if @current_user %>
              <li class="divider"></li>
              <li class="not-click <%= selected('posts') %>"><%= link_to 'Create sample posts', posts_url %></li>
              <li class="divider"></li>
              <li class="not-click <%= selected('users') %>"><%= link_to 'Visit sample users', users_url %></li>
              <li class="divider"></li>
            <% end %>
          </ul>
          <!-- Right Nav Section -->
          <ul class="right">
            <li class='not-click'><%= link_to "Logged in as #{@current_user.name}", '#' if @current_user%></li>
            <li class="divider"></li>
            <li class='not-click'><%= link_to 'logout', logout_url if @current_user%></li>
          </ul>
        </section>
      </nav>
    </div>
    <a href="https://github.com/gouravtiwari/audit_rails_example/fork"><img style="position: fixed; top: 0; left: 0; border: 0; z-index: 1000;" src="https://s3.amazonaws.com/github/ribbons/forkme_left_orange_ff7600.png" alt="Fork me on GitHub"></a>
    
    <br/>
    <br/>
    <br/>
    <div class="row">
      <%= yield %>
    </div>

    <%= javascript_include_tag "application" %>
  </body>
</html>
